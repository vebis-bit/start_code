from oppbevaring import *
    
class Lager():
    def __init__(self):
        self.kjøleskap = {
            "eple":150,
        }
    def add_ingredients(self,liste):
        for i in range (len(liste)):
            for j in liste[i][5]:
                matvare = j['food']
                vekt = j['weight']
                print(vekt, matvare)

    


#def lag_oppskrift(lager, oppskrit):
 #   for key,value in oppskrit.items():
  #      if key in lager:
   #         lager[key] -= value
    #    else:
     #       lager[key] = -value
    #return lager
#print(lag_oppskrift(Lager, fruktsalat))


